name: Data Quality Smoke

on:
  push:
    paths:
      - 'src/local_api/data_quality.py'
      - 'src/local_api/sheet_mapping_server.py'
      - 'scripts/test_data_quality_api_smoke.py'
      - '.github/workflows/data-quality-smoke.yml'
  pull_request:
    paths:
      - 'src/local_api/data_quality.py'
      - 'src/local_api/sheet_mapping_server.py'
      - 'scripts/test_data_quality_api_smoke.py'
      - '.github/workflows/data-quality-smoke.yml'

jobs:
  data-quality-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install duckdb

      - name: Run data quality API smoke tests
        run: python scripts/test_data_quality_api_smoke.py
