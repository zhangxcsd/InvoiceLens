# DWD 明细：货物名称（hwlwmc）中命中以下任一子串时，可识别为「详见销货清单」类汇总参考行，
# 用于 logic_line_no = 0（同票≥2 行时取排序首条命中行；详见 src/etl/cleaner.py）。
#
# 规则：
# - 编辑本列表即可，无需改程序；保存后下次跑 ODS→DWD 生效。
# - ETL 对名称列做了 LOWER() 再匹配；短语按子串匹配（LIKE '%短语%'）。
# - 每项为纯文本子串，勿含单引号；若需更多变体，逐行追加即可。

- 详见销货清单
- 详见销售清单
